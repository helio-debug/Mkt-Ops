---
description: SpyOps — espiona anúncios de um concorrente na Meta Ad Library (copy, funil, LPs, vídeos)
---

Use a skill `spyops` com o alvo em `$ARGUMENTS` (URL da Meta Ad Library, domínio/keyword ou page id).
Regras: pipeline Puppeteer, NUNCA Playwright, NUNCA a Graph API. Saída em `analises/<slug>/`. Baixar amostra de vídeos e analisar.
