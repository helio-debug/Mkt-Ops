# /spy — SpyOps de anúncios + funil

Use a skill `spyops` com o alvo passado em `$ARGUMENTS` (URL da Meta Ad Library, domínio/keyword ou page id).

Regras (detalhe na skill): pipeline Puppeteer, **nunca Playwright**, **nunca a Graph API** (travada). Saída em `analises/<slug>/`. Baixar amostra de vídeos e analisar.
